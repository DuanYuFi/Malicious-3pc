from Compiler.GC.types import *

a = sbit(1)
b = sbit(0)

ROUNDS = 500000

import random

for i in range(ROUNDS):
    random.choice([a, b]) & random.choice([a, b])

c = a & b

print_ln("%s", c.reveal())